% ============================================================
% Figure: Per-carrier entropy distribution (RQ2)
% ============================================================
\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{latex/figures/carrier_bits.pdf}
\caption{\textbf{Per-carrier realized capacity}.
Bars report bits embedded by carrier across three \locomo{} model--backend
settings. The payload is fully
realized in every setting, with \texttt{semantic\_realization} carrying
the bulk on \graphiti{} and a more even split across the three carriers
on \amem{}. }
\label{fig:carrier_bits}
\end{figure}
