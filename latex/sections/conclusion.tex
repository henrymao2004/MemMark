% ============================================================
% Conclusion — attribution-first, trade-off honest.
% ============================================================
\section{Conclusion}
\label{sec:conclusion}

\todo{\memmark{} delivers a capability no prior watermark or
provenance scheme provides: \textbf{snapshot-only writer
attribution} (R3 bit\_recovery = $1.00$) under the
adversarial-storage threat model, where field-stamping provenance
is forgeable and trajectory-bound watermarks have no signal left to
read. The price is a controlled $\sim$7\% relative F1 trade-off
(Table~\ref{tab:tradeoff}), in line with text-layer watermarks and
strictly preferable to the $0$ attribution rate of every
alternative in the forensic setting. Code, audit traces, and the
nine-attack benchmark are released.}
